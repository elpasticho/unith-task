FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] structlog httpx

COPY receiver/ ./receiver/

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1

CMD ["uvicorn", "receiver.main:app", "--host", "0.0.0.0", "--port", "9001"]
